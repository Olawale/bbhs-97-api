development:
    clients:
        default:
            database: bbhs_97
            hosts:
                - localhost:27017

production:
  sessions:
    default:
      uri: <%= ENV['MONGODB_URI'] %>
      options:
        skip_version_check: true
        safe: true